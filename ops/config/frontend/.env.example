# secrets/frontend — copy to .env.dev, .env.prod, .env.deploy (see secrets/.env.example)

# .env.dev — Vite/npm run dev (--mode dev)
VITE_API_URL=http://localhost:8090

# .env.prod — Vite build (--mode prod)
# VITE_API_URL=https://chat-api.example.ge

# .env.deploy — copy from .env.deploy.example
# DEPLOY_HOST_PORT=5177
# DEPLOY_PATH=/home/user/geostat/frontend
# DEPLOY_LAYOUT=structured
# DEPLOY_PATH_MODE=base
