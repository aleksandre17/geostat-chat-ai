# generated by geostat config-gen — do not edit
server:
  port: ${RETRIEVAL_PORT:8092}

spring:
  application:
    name: retrieval-service
  config:
    import: optional:classpath:application-custom.yml
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

management:
  endpoints:
    web:
      exposure:
        include: health,info
