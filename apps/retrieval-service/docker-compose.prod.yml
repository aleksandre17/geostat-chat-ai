# GENERATED — do not edit. Run: geostat compose-gen

# Prod — geostat ret deploy all --prod
services:
  geostat-chat-ai-retrieval:
    image: geostat-chat-ai-retrieval
    container_name: geostat-chat-ai-retrieval
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${RETRIEVAL_PORT:-8092}:${RETRIEVAL_PORT:-8092}"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      JAVA_TOOL_OPTIONS: >-
        -XX:+UseContainerSupport
        -XX:MaxRAMPercentage=75.0
        -XX:+UseG1GC
        -Xms256m -Xmx1g
    env_file:
      - ../../ops/config/retrieval/.env.prod
    networks:
      - geostat-chat-ai-net
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:${RETRIEVAL_PORT:-8092}/health | grep -q UP || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 90s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"

networks:
  geostat-chat-ai-net:
    driver: bridge

