# tools/

CLI **shim** — იგივე ბრძანებები, რაც `ops/cli/geostat.ps1`.

```powershell
.\tools\geostat.ps1 help
.\tools\geostat.ps1 init
```

პაკეტი: `kits/geostat-kit/` — [docs/KITS-PACKAGE.md](../docs/KITS-PACKAGE.md)

Canonical path (optional): `.\ops\cli\geostat.ps1`
