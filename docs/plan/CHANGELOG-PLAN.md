# გეგმის ცვლილებების ჟურნალი

| თარიღი | ცვლილება |
|--------|----------|
| 2026-05-25 | **RAG spec §3–§22:** Flyway V9–V16 map; P1 implementation status; Ops REST API; config flags; acceptance checklist Phase A/B/C; gemini-2.5-flash-lite |
| 2026-05-25 | **P1 architecture completion doc:** [PHASE-8-P1-ARCHITECTURE-COMPLETION.md](PHASE-8-P1-ARCHITECTURE-COMPLETION.md) — layer map, SOLID, cutover FSM, manifest `modules.*.derivation` + `chat-api.catalog` |
| 2026-05-25 | **P1 tests/ops:** `EnrichmentBackfillServiceTest`; backend `.env.dev` derived catalog cutover hints; `rag-p1-cutover` backfill status in `-Step status`; BACKLOG **P0-config-enrichment** |
| 2026-05-21 | ფაზა 1 **done**: B skeleton — retrieval, ingestion, platform-contracts |
| 2026-05-21 | ფაზა 7 **done**: `.cursor/skills`, `owner-standards` rule |
| 2026-05-21 | ADR-009 Accepted: Architecture B (separate deployables) |
| 2026-05-21 | **approved**: ფაზები 2–4 (chat→retrieval, ingestion pipeline, Qdrant) |
| 2026-05-21 | **rejected** (BACKLOG B-08): `libs/` → `apps/` — libs root-ზე რჩება |
| 2026-05-21 | დაემატა [SOURCE-RAG-DESIGN-PROJECTS-FILES.md](SOURCE-RAG-DESIGN-PROJECTS-FILES.md) — 8 სკრინშოტის დეტალური ამოღება + Q-01…Q-13 |
| 2026-05-21 | დაემატა [INFRA-DATA-STORES.md](INFRA-DATA-STORES.md) — Postgres/Redis/Qdrant, ფაზები, Q-14…Q-17 |
| 2026-05-21 | დაემატა [HYBRID-DEV-ARCHITECTURE.md](HYBRID-DEV-ARCHITECTURE.md) — **approved** D-01…D-06, ფაზა 0b, P0-infra-* |
| 2026-05-21 | დაემატა [DOCKER-ECOSYSTEM.md](DOCKER-ECOSYSTEM.md) — **approved** D-07…D-09, `geostat-chat-ai-net`, compose A/B, Q-16 closed |
| 2026-05-21 | **იმპლემენტაცია P0-infra:** `ops/compose/infra/` compose + `ops/config/infra/` + `geostat infra` (local/remote/tunnel) |
| 2026-05-21 | დაემატა [SERVER-DEPLOY-LAYOUT.md](SERVER-DEPLOY-LAYOUT.md) — **D-10** `geostat/{frontend,backend,infra}`, არა artifact, multi-project |
| 2026-05-22 | **ფაზა 0c done:** geostat-kit manifest audit #1–#7 (P0-kit-01…08) — compose-gen N-module, stack-deploy, CI health matrix |
| 2026-05-22 | **P6-01…03 done:** `ops/compose/stack/`, `ci.healthModules`, integration-stack manifest-driven |
| 2026-05-22 | **სერვერი:** infra გადავიდა `infra/geostat-chat-ai/`; წაშლილი ძველი `infra/compose/` + artifact dirs |
| 2026-05-22 | **Q-17 closed:** worker role = `ingestion`; `features.worker: false` |
| 2026-05-22 | **B-25 done:** StructureLookup წაშლილი; clarification/RAG — მხოლოდ ingestion pipeline; `zero-gap-architecture` rule + skills |
| 2026-05-22 | **R-01…R-06 done:** ChatResponse v2 — citations metadata, server-only telemetry, frontend grounded badge |
| 2026-05-22 | **P3-05 started:** ingestion-service Flyway schema `ingestion`, JPA, hybrid/docker profiles — [INGESTION-DATA-MODEL.md](INGESTION-DATA-MODEL.md) |
| 2026-05-22 | **Q-14 closed:** shared Postgres cluster, schema per service |
| 2026-05-22 | **P3-05 done:** ingestion JPA (6 tables), `POST/GET /jobs`, async crawl runner + Jsoup |
| 2026-05-22 | **P3-01 started:** PG `url_frontier` queue, link discovery from corpus policy |
| 2026-05-22 | **Skills/rules:** `owner-approved-stack` — plan libs (crawler4j, Jsoup), CA adapters; P3-01 = hybrid not optional |
| 2026-05-22 | **B-27 done:** Prompt YAML (`resources/prompts/`), `GoogleGenAiChatOptions` JSON schema, enriched session turns, clarification URL whitelist, stream intro extractor, grounded fix, `ops/ci/chat-prompt-smoke.ps1` |
| 2026-05-22 | **B-27 completion:** P6 `ExplanationGroundingVerifier`, H2 RAG excerpts in history, Gemini safety settings, prompt hash telemetry, `PromptBudgetTrimmer`, `ChatServiceTest`, OPS runbook |
| 2026-05-23 | **Hybrid ④ gap + kit upstream:** **P0-infra-08** consumer stopgap `ops/ci/hybrid-boot-app.ps1`; **P0-kit-12** `geostat hybrid boot` + `fe`/`be`/`ing`/`ret` `run` in `kits/geostat-kit` — [HYBRID-DEV-ARCHITECTURE.md](HYBRID-DEV-ARCHITECTURE.md) §6, **B-20** |
| 2026-05-23 | **P5-02 done:** `ops/ci/rag-pipeline-smoke.sh`; hybrid crawl→index→retrieval verified |
| 2026-05-23 | **P5-03 done:** `CatalogRagLinkMerger`, RAG prompt citations, `ops/ci/chat-catalog-rag-smoke.ps1` — **B-15** |
| 2026-05-23 | **P0-kit-09 baseline:** `geostat config-gen`, `application-custom.yml` split; ingestion golden + validate drift |
| 2026-05-22 | **P0-kit-09 retrieval:** `configGen.mode=simple` — qdrant-only `application.yml` + `application-custom.yml`; pytest 8/8 |
| 2026-05-22 | **P0-kit-09…11 done:** backend `env-profiles` (local/dev/prod), `API_PORT`, hybrid `springProfiles=local`; 3 modules validate |
| 2026-05-22 | **P0-infra-07 done:** `vscode_gen` hybrid compound + `geostat: infra tunnel` background preLaunchTask |
| 2026-05-23 | **B-16 / P2-02 done:** prod `RETRIEVAL_ENABLED`, docker DNS retrieval URL, gemini embeddings, RabbitMQ async; fixes: config-gen rabbit placeholders, `RabbitMqConfiguration` ConnectionFactory + RabbitAdmin |
| 2026-05-23 | **B-07 done:** `ops/ci/chat-rag-e2e-smoke.sh`; CI `integration-stack.sh` + `RUN_RAG_SMOKE=1`; manifest `ci.chatRagE2eSmoke` |
| 2026-05-23 | **B-17 done:** SSE `/api/chat/stream` (`ChatStreamController`, `ChatService.streamChatResponse`); frontend SSE + sync fallback |
| 2026-05-23 | **B-18 done:** `ConversationHistory` port; Redis adapter prod; Caffeine default local |
| 2026-05-23 | **B-19 done:** feedback API, retrieval hit telemetry, `turnId` on `ChatResponse`; `ops/ci/verify-services-prod.sh` |
| 2026-05-23 | **P6-migrate / B-12 done:** server structured layout verified; `geostat layout migrate --prod`; prod redeploy api+retrieval+ingestion; `verify-services-prod.sh` ALL PASSED; legacy `geostat-chat-api` container name coexists (not stopped) |
| 2026-05-23 | **Kit fix:** `stack-remote.sh` CRLF strip; `deploy.sh` UTF-8 BOM removed; `common.sh` service arg `\r` strip — fixes `stack-deploy` `all\r` error |
| 2026-05-23 | **Kit deploy preflight:** `check.sh` manifest port env + module Dockerfile; `stack-deploy` passes `@CliRest`; `config_gen --port-env`; ingestion shared gradlew |
| 2026-05-23 | **Kit stack-deploy:** java-boot-only `--prod`/`--skip-checks`; node-vite `-Environment` binding in `geostat.ps1`; `fe deploy dist` prod verified |
| 2026-05-23 | **B-06 done:** manifest module id `backend` → `chat-api`; aliases `be`/`chat` preserved; secrets path unchanged |
| 2026-05-23 | **B-21 done:** chat-api logic packages `session/`, `chat/`, `speech/`, `structure/`; removed flat `service/` |
| 2026-05-23 | **B-22 done:** `libs/qdrant-client` — shared `QdrantClients`, `VectorCollectionNaming`, `QdrantOperationException` |
| 2026-05-23 | **Worker cleanup:** removed `apps/backend/worker` Gradle submodule (`features.worker: false` since B-05) |
| 2026-05-23 | **Kit validate:** `manifest.schema.json` — `ci.chatRagE2eSmoke`, `ci.verifyServicesProd`; `driver_api` resolves CLI aliases for `path`/`type`/`caps` |
| 2026-05-23 | **Kit Windows:** `lib/geostat-python.ps1` — `py -3` launcher fallback; fixes `geostat validate`/`compose-gen` when Store `python` alias is broken |
| 2026-05-23 | **ADR-010 Accepted:** benefit gate (D-18); Q-01,02,04,05,13,15 closed; B-02 rejected; B-03/04/11 → PLAN; B-13 cancelled; approved D-19…D-24 |
| 2026-05-23 | **OPS-02 tooling:** `GET …/corpora/{name}/quality` + `ops/ci/corpus-quality-audit.sh` — empty-body / chunk / vector metrics; P3-03b trigger |
| 2026-05-24 | **RAG gaps closed:** L07 semantic rerank, Redis retrieval cache, P3-03b Playwright refetch API, B-28 archive port, B-30 `chat.*` telemetry (profile `telemetry-db`), OPS-02 `rag-full-corpus-crawl.ps1` |
| 2026-05-24 | **RAG polish:** incremental freshness (`freshness-refresh`), S3/MinIO archive adapter, V7 prod eval set, QueryRouter RAG-first, `hybrid-jar-boot.ps1`, catalog `urlEn` YAML |
| 2026-05-24 | **Closure:** kit JAR hybrid boot, catalog urlEn gen, chat broad smoke, eval MinPassRate, full corpus crawl started, Qdrant-locale/Playwright closed in plan |
| 2026-05-24 | **RAG-L11 done:** display metadata layer — V8 `document.display_description`, `PageDisplayMetadataExtractor`, Qdrant `pageDescription`, locale-aware citation snippets |
| 2026-05-24 | **Generic comparison:** [BORROW-FROM-GENERIC-RAG.md](BORROW-FROM-GENERIC-RAG.md) — 9 adoptable wins from generic blueprint (G-01..G-09): MVP flow doc, free dev path, Ollama RAM guide, cleaning checklist, near-duplicate B-31, scale gate B-32, multi-corpus B-33, realtime refresh B-34; ADR-010 conflicts explicitly excluded |
| 2026-05-24 | **Decision narrative:** [`transcripts/2026-05-24-derivation-architecture-decision.md`](transcripts/2026-05-24-derivation-architecture-decision.md) — owner→agent dialogue arc (initial state → senior review gaps → owner derivation insight → refined L1..L5 architecture → keyword nuance → adopted vs rejected variants → lessons for junior agents). Complement to ADR-011 (the *why* / *journey*; ADR captures the *what* / decision). |
| 2026-05-25 | **P1 cutover orchestrator:** `ops/ci/rag-p1-cutover.ps1` (`ci.ragP1Cutover`) — `-Step status|freeze|prep|gate|run`; `-WaitForDerived` auto-gate; cutover prep verifies `GET /catalog/status` after MV refresh; `PROJECT-PLAN.md` §A2 + RAG-U rows synced (P1 code done, runtime pending). |
| 2026-05-25 | **P1-15 step 0 + catalog MV status (spec §8):** `rag-eval-freeze-yaml-baseline.ps1` + `run-eval.py --write-yaml-frozen` (manifest `ci.ragEvalFreezeYamlBaseline`); V16 `catalog_view_refresh` audit; `GET /api/v1/ingestion/catalog/status` + `catalog_mv_fresh` readiness gate; refresh records timestamps. Tests: `CatalogViewStatusServiceTest`. |
| 2026-05-25 | **P1-15 dual baseline + eval gate catalog smoke:** `baseline.yaml-frozen.json` (pre-cutover YAML-era floor); `run-eval.py --reference-baseline` strict compare; `rag-eval-gate.ps1 -CompareYamlReference -RunCatalogSmoke`; cutover prep passes flags; `ops/eval/README.md` cutover checklist. Tests: `CatalogStatusControllerTest`. |
| 2026-05-25 | **CatalogResponseAssembler + single cluster match (RAG-U02):** `DerivedClusterMatch` + `DerivedCatalogReader.matchClusters` (one id+label round-trip); `YamlCatalogResponseAssembler` / `DerivedCatalogResponseAssembler` — labels+links in one assemble (no duplicate SQL per chat turn); `GET /api/v1/chat/catalog/status` for cutover ops; smoke script validates `source=derived`. Tests: `DerivedCatalogResponseAssemblerTest`. |
| 2026-05-25 | **Derived topic cluster labels (RAG-U02 cutover UX):** `CatalogTopicLabelResolver` port + yaml/derived strategies; `DerivedCatalogReader.matchTopicClusterLabels`; `{TOPICS}` prompt + `ChatResult.primaryTopic` use cluster labels (ka/en) when `source=derived`. Tests: `DerivedCatalogTopicLabelResolverTest`. |
| 2026-05-25 | **Derived catalog smoke + query alignment:** `ops/ci/chat-derived-catalog-smoke.ps1` (manifest `ci.chatDerivedCatalogSmoke`); `ChatService` catalog links from `retrievalQuery`; cutover prep `-RunCatalogSmoke`. |
| 2026-05-25 | **Derived catalog zero-gap wiring:** YAML loaders + `YamlTopicCatalog` gated on `source=yaml`; `DerivedMinimalTopicCatalog` when `source=derived` (no `topics.yaml` load); `ChatService` injects `CatalogLinkBuilder` directly; removed `ResponseBuilder` facade. Tests: `DerivedMinimalTopicCatalogTest`. |
| 2026-05-25 | **CatalogLinkBuilder strategy (RAG-U02 cutover):** port + `YamlCatalogLinkBuilder` / `DerivedCatalogLinkBuilder` selected by `geostat.chat.catalog.source`; removed `isDerived()` branching. |
| 2026-05-25 | **PresentationStyleCatalog port (SOLID §17):** static `TopicStyleCatalog` → `PresentationStyleCatalog` + `YamlPresentationStyleCatalog` adapter; DI in ResponseBuilder/PromptBuilder/CatalogRagLinkMerger/DerivedCatalogLinkBuilder; `cardType` per `page_kind` in YAML (no Java switch). Tests: `YamlPresentationStyleCatalogTest`. |
| 2026-05-25 | **topic-style.yaml Java loader (spec §17):** `TopicStyleCatalogLoader` loads `pageKinds` + `linkTypes` from YAML; removed hardcoded maps; derived link cards use page_kind styles. Superseded by PresentationStyleCatalog port row above. |
| 2026-05-25 | **P1 cutover prep script:** `ops/ci/rag-derivation-cutover-prep.ps1` — spec section 13 API sequence (authority, remine, cluster approve, catalog refresh, lifecycle sync, readiness); manifest `ci.ragDerivationCutoverPrep`; optional `-RunEvalGate`. |
| 2026-05-25 | **P1-15 derivation readiness API:** `GET …/corpora/{name}/derivation-readiness` — golden set, vector coverage, enrichment/aggregation gates (spec section 14); `rag-eval-gate.ps1` calls it before harness (`-AllowNotReady` for YAML-only baseline). |
| 2026-05-25 | **RAG-U12 P1-15 eval gate ops:** `ops/ci/rag-eval-gate.ps1` — health + golden set (>=150) + `query:analyze` probe + optional `lifecycle:sync-qdrant` + full harness; manifest `ci.ragEvalGate`. Ingestion `POST …/corpora/{name}/lifecycle:sync-qdrant` batch syncs Qdrant metadata without re-embed. Retrieval keyword SQL excludes `navigation` + curation `exclude`. |
| 2026-05-25 | **RAG-U12 eval completion + §17 lifecycle:** `DocumentServeStateResolver` + Qdrant payload `serveState/pageKind/scoreBoost`; `DocumentQdrantLifecycleSync` (index/enrichment/curation hooks); retrieval `CitationEligibilityFilter` (live/partial_enriched, exclude navigation); `POST /api/v1/chat/query:analyze` for intent/entity metrics; `run-eval.py` computes `intent_accuracy` + `entity_F1` via `--chat-base`; evaluation-queries API exposes `expectedEntities`; `.github/workflows/eval.yml` (unit + dry-run). |
| 2026-05-25 | **RAG-U05 Curation overlay API (Phase 8 Layer 4):** `GET/POST/DELETE /api/v1/ingestion/curation/overrides` — budget ≤50 active rows, reason mandatory, default TTL 90d, actions boost/demote/exclude/pin_as_portal/rename_topic; `CurationOverlayService` applies score_boost multipliers + catalog refresh on exclude/pin; upsert on `(url_hash, action)`. V15 `query_intent_cache` (RAG-U14 schema) + `mv_portal_link` rebuild with `pin_as_portal` priority. `JdbcDerivedCatalogReader` honors `rename_topic` label overrides. Added `topic-style.yaml` (presentation-only, §17). Unit tests: `CurationOverlayServiceTest`. |
| 2026-05-25 | **RAG-U02 chat-api derived catalog adapter:** `geostat.chat.catalog.source=yaml|derived` (default yaml); `DerivedCatalogReader` port + `JdbcDerivedCatalogReader` (`mv_portal_link`, `mv_specific_link`, `mv_topic_keywords`); `DerivedCatalogLinkBuilder` wired into `ResponseBuilder`; JDBC URL `GEOSTAT_CHAT_CATALOG_JDBC_URL`. YAML path unchanged until eval gate flip. Unit tests: derived link builder. |
| 2026-05-25 | **RAG-U02 Catalog views (Phase 8 Layer 3):** V13 `mv_portal_link`, `mv_specific_link`, `mv_topic_keywords` (approved `topic_cluster` only, curation `exclude` filter, unique indexes for CONCURRENTLY refresh); `CatalogViewRefreshService` + nightly `CatalogViewRefreshJob`; `POST /api/v1/ingestion/catalog:refresh`; `CatalogRefreshAfterBatch` hook after authority/topic batch. Default `geostat.ingestion.aggregation.enabled=false`. Unit tests: refresh SQL order + batch hook gates. |
| 2026-05-25 | **RAG-U01g TopicMiner + TopicAssigner (Phase 8):** V12 `topic_cluster` + `curation_override` + eval FK; `TopicMiner`/`TopicAssigner` ports; `SmileKMeansTopicMiner` (summary embedding k-means, k=√(N/10), Gemini labels via `topic-label.yaml`, `centroid_embedding` for assign); `EmbeddingNearestTopicAssigner` + `TopicAssignEnrichmentService` (orchestrator tail); batch `TopicRemineService` + nightly scheduler (`topic-scheduler-enabled=false`); `POST …/topics:remine`. Default `topic-min-documents=1000`. `com.github.haifengl:smile-core`. Unit tests: cluster count, k-means, label parse, nearest assign. |
| 2026-05-25 | **RAG-U01e AuthorityDeriver (Phase 8):** `AuthorityDeriver` port; `JGraphTPageRankAuthorityDeriver` (in-corpus href graph from `url_frontier`, damping 0.85, min-max normalize, freshness decay, excludes `page_kind=navigation`); `AuthorityRecomputeService` + nightly `AuthorityBatchScheduler` (`authority-scheduler-enabled=false` default); `POST …/corpora/{name}/authority:recompute` (501 when enrichment off). Batch-only — not in per-doc orchestrator. Unit tests: PageRank graph, composer, freshness, deriver persist. |
| 2026-05-25 | **RAG-U01h Title/Summary vectors (Phase 8 prep):** `DocumentVectorWriter` port; `QdrantNamedVectorWriter` (named `title`/`summary` on chunk points via `updateVectors`) + `NoOpDocumentVectorWriter` default; `TitleVectorEnrichmentService` + `SummaryVectorEnrichmentService` (summary_vector gated on completed summary run); orchestrator extended. `INGESTION_NAMED_VECTORS_ENABLED=false` until RAG-U08 collection migration. Unit tests: summary text pick + vector services. |
| 2026-05-25 | **RAG-U01f PageKindClassifier (Phase 8):** `PageKindClassifier` port + `PageKindResult`; `PageKindUrlHeuristic` (URL pre-filter) + `GeminiFewShotPageKindClassifier` + prompt `prompts/enrichment/page-kind.yaml`; `PageKindEnrichmentService`; orchestrator extended (summary → locale_pair → keywords → entities → page_kind). Closed set: portal/dataset/report/news/faq/navigation/unknown. Idempotency `gemini-2.0-flash-lite-pagekind@2026-05-25`. Unit tests: heuristic paths, JSON parse, service idempotency. |
| 2026-05-25 | **RAG-U01d LocalePairDeriver (Phase 8):** `LocalePairDeriver` port; `UrlPlusEmbeddingLocalePairer` (URL `/ka`↔`/en` + title embedding cosine ≥0.92 fallback); `LocalePairEnrichmentService` sets bidirectional `locale_pair_doc_id` + reuses `DocumentLocalePairLinker`; orchestrator order: summary → locale_pair → keywords → entities. Idempotency `url-embed-v1@2026-05-25`. Unit tests: URL/table/embedding paths + service bidirectional persist. |
| 2026-05-25 | **RAG-U01c EntityDeriver (Phase 8):** `EntityDeriver` port + `Entity` record; `GeminiFewShotEntityDeriver` + prompt `prompts/enrichment/entities.yaml`; `EntityEnrichmentService`; orchestrator extended (summary → keywords → entities); idempotency `gemini-2.0-flash-lite-entities@2026-05-25`. Unit tests: JSON parse/validation + service idempotency. |
| 2026-05-25 | **RAG-U01b KeywordDeriver (Phase 8):** `KeywordDeriver` port; `YakeKeywordDeriver` + minimal Java-native `YakeKeywordExtractor` (`yake-v1`, ngram 1–3, top-15, dedup); `KeywordEnrichmentService` + locale-pair merge; shared `EnrichmentRunExecutor`; `DocumentEnrichmentOrchestrator` (summary → keywords). Default still `INGESTION_ENRICHMENT_ENABLED=false`. Unit tests: extractor + service idempotency/locale merge. |
| 2026-05-25 | **RAG-U01a SummaryDeriver (Phase 8):** V11 `enrichment_run`; `GeminiSummaryDeriver` + `SummaryEnrichmentService` + prompt `prompts/enrichment/summary.yaml`; `DocumentPostPersistPipeline` (index + enrichment); async trigger (events off) + Rabbit `document.parsed` (events on); default `INGESTION_ENRICHMENT_ENABLED=false`. Unit tests: parse + service idempotency. |
| 2026-05-25 | **RAG-U07d/e complete (Phase 8 P1 step 14):** `EntityJsonParser` in platform-contracts (shared U01c/U07d); `HeuristicQueryEntityExtractor` + `GeminiQueryEntityExtractor` + `RoutingQueryEntityExtractor`; `GeminiQueryExpander` + `RoutingQueryExpander` (terminology + optional LLM); `AnalyzedQuery.entities`; pipeline stage order SpellFix→Normalize→Intent→Entity→Expand; sub-flags `gemini-entity-enabled` / `llm-expand-enabled` (default false). Prior: U07a/c adapters. |
| 2026-05-25 | **P8-plan-01 approved (planning gate):** სწორ დროს service/package split **planning review** — after Phase 8 P2 cutover + 30d prod telemetry (or early scale signals); D-27 updated; spec § 13.6; PROJECT-PLAN row + sequence step 26. Implementation remains B-37 observed triggers only. |
| 2026-05-25 | **D-27 approved:** service split deferred — 4 deployables unchanged for Phase 8; internal `enrichment/`/`catalog/`/`curation/` package layout in spec §5; B-37 enrichment-service extraction trigger (3 observed conditions, not planned). approved/README.md D-27; BACKLOG B-37; spec § 13.5 row. |
| 2026-05-25 | **Phase 8 spec extended (no architectural change):** [RAG-DERIVATION-ARCHITECTURE.md](RAG-DERIVATION-ARCHITECTURE.md) — borrowed clarity items from generic blueprint (hardware/Gemini cost excluded per owner): § 2 Layer 1 prerequisites contract, § 4 complete Qdrant payload contract, § 6.5 per-corpus refresh policy (`refresh_cron`/`realtime_enabled`), § 8 pipeline fallback chain (Gemini/Qdrant/rerank/mv-stale degraded modes), § 13.4 MVP cutover set (4 derivers minimum: U01a/e/f/g), § 13.5 scale thresholds (B-32/33/36 triggers), § 17 document lifecycle state machine, § 19 backlog ID index. BACKLOG.md: B-35 `UrlFrontierPort` abstraction, B-36 embedding-model migration playbook added (proposed). Section numbers preserved (1..19); zero code change. |
| 2026-05-24 | **ADR-011 Accepted + Phase 8 approved:** RAG **derivation architecture** — corpus is single source of truth; YAML catalog deprecated. Approved: D-25 (derivation adopted), D-26 (curation overlay ≤50 rows, TTL 90d, reason mandatory). Spec: [RAG-DERIVATION-ARCHITECTURE.md](RAG-DERIVATION-ARCHITECTURE.md) — full L1..L5 architecture, V9..V12 migrations, 8 derivers (RAG-U01a..h), 3 materialized views (mv_portal_link, mv_specific_link, mv_topic_keywords), curation overlay (`ingestion.curation_override`), query understanding pipeline (RAG-U07: SymSpell + Normalizer + Gemini IntentClassifier + EntityExtractor + QueryExpander), multi-vector Qdrant migration v1→v2 (RAG-U08), HyDE+multi-query (RAG-U09), hybrid retrieval + RRF fusion + MMR (RAG-U10), confidence tier + ResponseRouter (RAG-U11), eval harness 150–300 golden + CI gate (RAG-U12), feedback score_boost (RAG-U13), caching tier (RAG-U14). Free packages adopted: JGraphT, Smile, YAKE Java port, SymSpell. Rejected: original RAG-U01 raw YAML→DB migration (superseded), RAG-U03 SourceComposer (merged into U10), RAG-U04 hybrid keyword topic classifier (replaced by U07c IntentClassifier), RAG-U06 public catalog API (dropped), Python sidecars (deferred). RAG-U15 knowledge graph (Apache AGE) deferred to P4+. PROJECT-PLAN.md Phase 8 row + RAG-U01a..h, U02, U05, U07..U15 detailed rows added; approved/README.md D-25, D-26; BACKLOG.md rejected variants archived; ADR-011 doc; ADR-README index updated. |
| 2026-05-23 | **P0-kit-13 done:** `modules.*.compose.embeddedWorker`; `effective_compose_features`; catalog `features.worker` deprecated |
